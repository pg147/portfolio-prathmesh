export default function Separator({ className }) {
    return (
        <div className={`h-0.5 w-full bg-gradient-to-l from-separatorGradient/0 via-primary/70 to-separatorGradient/0 ${className}`}>

        </div>
    )
}
