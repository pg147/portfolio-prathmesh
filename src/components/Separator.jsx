export default function Separator({ className }) {
    return (
        <div className={`h-[1px] w-full bg-gradient-to-l from-separatorGradient/0 via-primary/70 to-separatorGradient/0 ${className}`}>

        </div>
    )
}
